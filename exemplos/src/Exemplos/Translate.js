export function translator(text){
	if(text === 'Clean code') return "Codigo limpo"
}

// no tsconfig:
/*
	Se no tsconfig estiver habilitada a opcao: declaration como TRUE, ele gera o d.ts automaticamente
*/